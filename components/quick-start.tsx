"use client";
import {useState,useEffect} from 'react';import {useRouter} from 'next/navigation';import {Modal} from './modal';
const steps=[['项目','填写品牌、目标人群和营销目标。'],['热点雷达','搜索近期热点，并与去年同期趋势进行对比。'],['趋势洞察','从热点变化中找到值得品牌进入的机会。'],['创意与内容','生成创意路线，并继续产出文案、主视觉、视频和达人Brief。']];
export function QuickStart(){const [open,setOpen]=useState(false),[step,setStep]=useState(0);const router=useRouter();useEffect(()=>{try{setOpen(!localStorage.getItem('marketing-quick-start'))}catch{setOpen(true)}},[]);function close(){try{localStorage.setItem('marketing-quick-start','done')}catch{}setOpen(false)}return <><button aria-label="快速开始帮助" className="btn-light" onClick={()=>{setStep(0);setOpen(true)}}>?</button>{open&&<Modal title="第一次使用 AI营销工作台？" onClose={close}><p className="my-5">从一个品牌Brief开始，AI会带你走完整个营销策划流程。</p><div className="flex gap-2">{steps.map((s,i)=><button className={i===step?'btn':'btn-light'} key={s[0]} aria-label={'查看第'+(i+1)+'步'} aria-pressed={i===step} onClick={()=>setStep(i)}>{i+1}</button>)}</div><section className="panel my-6 p-6"><p className="eyebrow">STEP {step+1} / Quick Start</p><h2 className="my-4">{steps[step][0]}</h2><p>{steps[step][1]}</p></section><div className="flex flex-wrap gap-3">{step<3&&<button className="btn-light" onClick={()=>setStep(step+1)}>下一步</button>}<button className="btn" onClick={()=>{close();router.push('/')}}>开始体验</button><button className="btn-light" onClick={close}>跳过</button></div></Modal>}</>}
